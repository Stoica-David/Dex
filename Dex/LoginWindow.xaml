<Window x:Class="Dex.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login Window" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Label Content="Insert your credentials:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="99,10,0,0" FontWeight="Bold" FontSize="16"/>

        <!-- Username Label and TextBox -->
        <Label Content="Username:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,56,0,0"/>
        <TextBox x:Name="usernameTextBox" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0" Width="200"/>

        <!-- Password Label and PasswordBox -->
        <Label Content="Password:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="14,120,0,0"/>
        <PasswordBox x:Name="passwordBox" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,124,0,0" Width="200"/>

        <!-- Log In Button -->
        <Button Content="Log In" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="152,180,0,0" Width="76" Click="Login_Click"/>
    </Grid>
</Window>