<UserControl x:Class="Dex.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dex.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Label Content="Insert your credentials:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="99,10,0,0" FontWeight="Bold" FontSize="16"/>

        <!-- Username Label and TextBox -->
        <Label Content="Username:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,56,0,0"/>
        <TextBox x:Name="usernameTextBox" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0" Width="200" Text="{Binding Username}"/>

        <!-- Password Label and PasswordBox -->
        <Label Content="Password:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="14,120,0,0"/>
        <TextBox x:Name="passwordBox" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,124,0,0" Width="200" Text="{Binding Password}"/>

        <!-- Log In Button -->
        <Button Content="Log In" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="152,180,0,0" Width="76" Command="{Binding SwitchToAdministrativeCommand}"/>
    </Grid>
</UserControl>
